name: rift
description: >-
  Расселина — idle-RPG автобатлер. Ядро симуляции: чистый Dart, без Flutter.
  Flutter-слой добавляется в Фазе 3 (`flutter create .` поверх этого пакета).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0

dev_dependencies:
  test: ^1.25.0
